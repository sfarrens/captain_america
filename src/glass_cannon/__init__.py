"""GLASS-based cosmology utilities and pipelines.

Subpackages and modules provide:

- Cosmology setup helpers for CAMB and GLASS compatibility (`cosmo_setup`).
- Angular power spectrum computation via CAMB (`Cls`).
- Matter field generation helpers (`matter`).
- High-level simulation pipeline (`glass_pipeline`).
- Galaxy distribution utilities (`galaxies`).
- HI 21 cm tracer utilities (`HI_tracer`).
"""
